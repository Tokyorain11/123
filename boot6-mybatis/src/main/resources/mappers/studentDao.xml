<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.all.boot6mybatis.dao.studentDao">
    <select id="selectByID" resultType="com.all.boot6mybatis.domain.student">
        select * from stu_inf.stu_tab where id=#{stuID}
    </select>
</mapper>